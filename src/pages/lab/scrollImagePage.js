import React from "react";
import Layout from "../../components/layout";
import ScrollImage from "../../components/scrollImage/scrollImage";

const scrollImagePage = () => {
    return (
        <Layout>
            <ScrollImage />
        </Layout>
    );
}

export default scrollImagePage;
